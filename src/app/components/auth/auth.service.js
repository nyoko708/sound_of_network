(function() {
  'use strict';

  angular
    .module('sound_of_network')
    .factory('search', search);

  /** @ngInject */
  function search($http, $location) {

    var service = {
    };

    return service;
  }

})();
